# CMakeLists files in this project can
# refer to the root source directory of the project as ${DumplingBot_SOURCE_DIR} and
# to the root binary directory of the project as ${DumplingBot_BINARY_DIR}.
cmake_minimum_required (VERSION 2.8.3)
project (DumplingBot)

add_subdirectory (localization)
